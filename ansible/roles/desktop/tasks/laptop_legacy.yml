---
- name: install legacy laptop utils
  package:
    name:
      - powertop
      - thermald
      - tlp
      - tlp-rdw
    state: present

- name: enable legacy laptop services
  systemd: name={{item}} enabled=yes
  with_items:
    - thermald
    - tlp
