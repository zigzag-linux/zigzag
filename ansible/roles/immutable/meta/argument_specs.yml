---
argument_specs:
  main:
    short_description: Configuration stage
    options:
      conf_stage:
        required: true
        choices:
          - image_setup
          - system_install
          - system_install_late
        description: "The stage at which the role is being run"
